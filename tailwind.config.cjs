const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        bebas: ["bebas", "sans-serif"],
      },
    },
  },

  plugins: [],
};

module.exports = config;
